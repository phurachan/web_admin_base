# Test Cases - เอกสารการทดสอบ

เอกสารการทดสอบ (Manual Testing) สำหรับทุก feature ในระบบ Web Admin Base

---

## 📋 สารบัญ

- [โครงสร้างเอกสาร](#โครงสร้างเอกสาร)
- [รายการ Test Cases](#รายการ-test-cases)
- [วิธีการใช้งาน](#วิธีการใช้งาน)
- [Template](#template)
- [Slash Command](#slash-command)

---

## โครงสร้างเอกสาร

```
docs/test-cases/
├── README.md                          # เอกสารนี้
├── TEMPLATE.md                        # Template สำหรับสร้าง Test Case
└── features/                          # Test Cases แยกตาม Feature
    ├── feature1.test.md              # Test Case: Feature 1 (Markdown)
    ├── feature1.test.csv             # Test Case: Feature 1 (CSV)
    ├── [feature-name].test.md        # Test Case อื่นๆ
    └── [feature-name].test.csv       # CSV สำหรับ export
```

---

## รายการ Test Cases

### ✅ Features ที่มี Test Cases แล้ว

| Feature | Module | Test Cases | Markdown | CSV | Status |
|---------|--------|------------|----------|-----|--------|
| จัดการระบบย่อย 1 (Feature 1) | Manages | 15 | [feature1.test.md](features/feature1.test.md) | [feature1.test.csv](features/feature1.test.csv) | ✅ Done |

### 📝 Features ที่ต้องสร้าง Test Cases

| Feature | Module | Priority | Status |
|---------|--------|----------|--------|
| จัดการผู้ใช้งาน | Admin | High | ⏳ Pending |
| จัดการสิทธิ์ | Admin | High | ⏳ Pending |
| จัดการเมนู | Admin | Medium | ⏳ Pending |

> **หมายเหตุ**: เพิ่ม Test Cases ใหม่ด้วย slash command `/generate-test-case`

---

## วิธีการใช้งาน

### 1. เลือก Test Case ที่ต้องการทดสอบ

เปิดไฟล์ Markdown หรือ CSV:
- **Markdown**: อ่านง่าย มี format สวยงาม
- **CSV**: import เข้า Excel, Google Sheets ได้

### 2. ทดสอบตาม Test Case

แต่ละ Test Case มี:
- **Objective**: วัตถุประสงค์การทดสอบ
- **Pre-condition**: เงื่อนไขก่อนทดสอบ
- **Test Data**: ข้อมูลที่ใช้ทดสอบ
- **Steps**: ขั้นตอนการทดสอบทีละ step

### 3. บันทึกผลลัพธ์

ในแต่ละ step:
1. บันทึก **Actual Result** (ผลลัพธ์จริงที่เกิดขึ้น)
2. เลือก **Status**:
   - ✅ **Pass** - ผ่านตามที่คาดหวัง
   - ❌ **Fail** - ไม่ผ่าน มีปัญหา
   - ⏭️ **Skipped** - ข้ามไม่ทดสอบ
3. เขียน **Remark** (ถ้ามีข้อสังเกต)

### 4. สรุปผลการทดสอบ

อัปเดตตารางสรุปท้ายเอกสาร:
- Total Test Cases
- Passed
- Failed
- Skipped
- Pass Rate

---

## ตัวอย่างการทดสอบ

### ตัวอย่างที่ 1: ทดสอบด้วย Markdown

```markdown
## TC-F1-001: แสดงรายการ Feature 1 ทั้งหมด

### ขั้นตอนการทดสอบ
| Step | Action | Expected Result | Actual Result | Status |
|------|--------|-----------------|---------------|--------|
| 1 | เข้าสู่หน้า "จัดการระบบย่อย 1" | ระบบแสดงหน้าจัดการระบบย่อย 1 | ✅ แสดงหน้าถูกต้อง | ✅ Pass |
| 2 | ตรวจสอบ Page Header | แสดงหัวข้อ "จัดการระบบย่อย1" | ✅ แสดงถูกต้อง | ✅ Pass |
```

### ตัวอย่างที่ 2: ทดสอบด้วย CSV (Excel/Google Sheets)

1. เปิดไฟล์ `feature1.test.csv` ใน Excel
2. กรอก **Actual Result** ในคอลัมน์ที่กำหนด
3. เลือก **Status** (Pass/Fail/Skipped)
4. บันทึกไฟล์

#### รูปแบบ CSV สำหรับ Merge Cells

ไฟล์ CSV ถูกออกแบบให้พร้อมสำหรับการ merge cells ใน Excel/Google Sheets:

```csv
Test Case ID,Test Case Name,Objective,Pre-condition,Step,Action,Expected Result,Actual Result,Status,Tester,Test Date,Remark
TC-F1-001,แสดงรายการ Feature 1 ทั้งหมด,ตรวจสอบการแสดงรายการข้อมูล,ผู้ใช้ login แล้ว,1,เข้าสู่หน้า "จัดการระบบย่อย 1",ระบบแสดงหน้าจัดการระบบย่อย 1,,,,,,
,,,,,2,ตรวจสอบ Page Header,แสดงหัวข้อ "จัดการระบบย่อย1",,,,,,
,,,,,3,ตรวจสอบปุ่มต่างๆ,มีปุ่ม "+ เพิ่ม Feature 1",,,,,,
TC-F1-002,ค้นหา Feature 1,ตรวจสอบการค้นหาข้อมูล,ผู้ใช้อยู่ที่หน้าจัดการระบบย่อย 1,1,คลิกช่องค้นหา,Focus ที่ช่องค้นหา,,,,,,
```

**คำอธิบาย**:
- **Test Case ID** และ **Test Case Name** จะแสดงเฉพาะ **row แรก** ของแต่ละ Test Case
- Rows ถัดไปของ Test Case เดียวกันจะเว้นว่าง (commas เท่านั้น) เพื่อให้พร้อมสำหรับ merge cells
- เมื่อเปิดใน Excel สามารถ merge cells ได้ทันทีโดยไม่ต้องแก้ไขข้อมูล
- ช่วยให้อ่านง่ายและดูสวยงามขึ้นเมื่อ import ลง Excel/Google Sheets

---

## Template

ใช้ [TEMPLATE.md](TEMPLATE.md) สำหรับสร้าง Test Case ใหม่

### โครงสร้าง Template:

```markdown
# Test Case: [ชื่อ Feature]

## ข้อมูลทั่วไป
- Feature, Module, URL, Priority, Created Date

## TC-XXX-001: [ชื่อ Test Case]

### ข้อมูลทดสอบ
- Objective
- Pre-condition
- Test Data

### ขั้นตอนการทดสอบ
| Step | Action | Expected Result | Actual Result | Status |
|------|--------|-----------------|---------------|--------|

### Post-condition
```

---

## Slash Command

สร้าง Test Case ใหม่อัตโนมัติด้วย:

```bash
/generate-test-case
```

Claude จะถามข้อมูล feature และสร้างไฟล์ให้อัตโนมัติ:
- Markdown (`.test.md`)
- CSV (`.test.csv`)

### ข้อมูลที่ Claude จะถาม:

1. ชื่อ feature (ภาษาไทยและอังกฤษ)
2. URL/Path
3. Module
4. Priority
5. ฟีเจอร์ที่มี (List, Search, Filter, Create, Edit, Delete, etc.)

### ผลลัพธ์:

```
สร้าง Test Case สำเร็จ!
- จำนวน Test Cases: 15
- ไฟล์:
  - Markdown: docs/test-cases/features/user_management.test.md
  - CSV: docs/test-cases/features/user_management.test.csv
```

---

## Best Practices

### 1. **ทดสอบตาม Priority**
- **High**: ทดสอบก่อน (CRUD, Authentication)
- **Medium**: ทดสอบรอง (Search, Filter)
- **Low**: ทดสอบทีหลัง (UI/UX)

### 2. **บันทึกผลละเอียด**
- ถ่ายภาพหน้าจอ error
- บันทึก error messages
- เขียน Remark ชัดเจน

### 3. **ทดสอบทั้ง Happy Path และ Error Cases**
- Happy Path: กรณีปกติ สำเร็จ
- Error Cases: กรณี validation, error

### 4. **ทดสอบซ้ำเมื่อแก้ bug**
- Re-test Test Cases ที่ fail
- ตรวจสอบว่าไม่มี regression

### 5. **อัปเดตเอกสารเมื่อ feature เปลี่ยน**
- แก้ไข Test Cases เมื่อ feature เปลี่ยนแปลง
- เพิ่ม Test Cases ใหม่เมื่อมี feature ใหม่

---

## รูปแบบการรายงานผล

### รายงานผลแบบสรุป

```
Feature: จัดการระบบย่อย 1
Test Date: 2025-11-13
Tester: John Doe

Summary:
- Total: 15 Test Cases
- Passed: 13 (86.7%)
- Failed: 2 (13.3%)
- Skipped: 0

Failed Test Cases:
- TC-F1-006: เพิ่ม Feature ใหม่ (Validation Error)
  - Issue: Error message ไม่แสดง
- TC-F1-013: อัปโหลดรูปภาพ
  - Issue: อัปโหลดไม่สำเร็จ ขนาดไฟล์ใหญ่
```

---

## FAQ

### Q: Test Case ควรละเอียดแค่ไหน?
A: ละเอียดพอที่คนอื่นอ่านแล้วทดสอบตามได้ โดยไม่ต้องถามเพิ่ม

### Q: ควรทดสอบบ่อยแค่ไหน?
A:
- **Regression Testing**: ทุกครั้งที่มี major update
- **Smoke Testing**: ทุกครั้งที่ deploy production
- **Full Testing**: ก่อน release version ใหม่

### Q: ถ้า Test Case เก่าไม่ตรงกับ feature ปัจจุบัน?
A: อัปเดต Test Case ให้ตรงกับ feature ปัจจุบัน และบันทึกการเปลี่ยนแปลงใน "Last Updated"

### Q: ควรใช้ Markdown หรือ CSV?
A:
- **Markdown**: อ่านง่าย สำหรับทดสอบทีละ Test Case
- **CSV**: import Excel ได้ สำหรับรายงานผลหลาย Test Cases พร้อมกัน

### Q: ทำไม CSV มี Test Case ID และ Test Case Name แค่ row แรก?
A: ออกแบบให้พร้อมสำหรับ merge cells ใน Excel/Google Sheets โดย:
- Row แรกของแต่ละ Test Case มีข้อมูล Test Case ID และ Test Case Name
- Rows ถัดไปเว้นว่าง (commas) เพื่อให้ merge cells ได้ทันที
- เมื่อเปิดใน Excel สามารถเลือกช่องที่ว่างและกด "Merge & Center" ได้เลย

---

## สัญลักษณ์ที่ใช้

| สัญลักษณ์ | ความหมาย |
|----------|----------|
| ⬜ | Not Tested (ยังไม่ทดสอบ) |
| ✅ | Pass (ผ่าน) |
| ❌ | Fail (ไม่ผ่าน) |
| ⏭️ | Skipped (ข้าม) |
| ⏳ | Pending (รอดำเนินการ) |
| 🔄 | In Progress (กำลังดำเนินการ) |

---

## เครื่องมือแนะนำ

### สำหรับ Markdown:
- **VS Code** + Markdown Preview
- **Obsidian**
- **Notion**

### สำหรับ CSV:
- **Microsoft Excel**
- **Google Sheets**
- **LibreOffice Calc**

### สำหรับจัดการ Test Cases:
- **TestRail** (ถ้าต้องการ Test Management Tool)
- **Jira** + Xray (สำหรับ Agile Team)
- **Excel/Google Sheets** (Simple และฟรี)

---

## การอัปเดตเอกสาร

เมื่อมีการเปลี่ยนแปลง:

1. อัปเดตไฟล์ Test Case
2. เปลี่ยน "Last Updated" ในหัวเอกสาร
3. อัปเดตตาราง "รายการ Test Cases" ใน README นี้
4. Commit changes พร้อม message ชัดเจน

```bash
git add docs/test-cases/
git commit -m "docs: update test cases for Feature 1"
```

---

## Contact

หากมีคำถามหรือข้อเสนอแนะเกี่ยวกับเอกสาร Test Cases:
- สร้าง Issue ใน GitHub
- ติดต่อ QA Team
- แก้ไขเอกสารและส่ง Pull Request

---

**Last Updated**: 2025-11-13
